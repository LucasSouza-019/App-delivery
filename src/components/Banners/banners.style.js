import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles = {
    icone:{
        width: 220,
        height: 120,

    },
    banner:{
        paddingRight: 14,
        marginTop: 15,
        marginBottom: 15
    }
}